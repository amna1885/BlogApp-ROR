<%= form_with(model: [@post, @suggestion], local: true, class: "ui form") do |form| %>
  <%= form.text_area :content, placeholder: "Edit your suggestion" %>
  <%= form.submit "Update Suggestion", class: "ui button" %>
<% end %>
